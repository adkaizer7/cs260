//@module

var Enum = require("modules/meta/enum");

/**
 * Tests for the enum module. Takes a Logger for OUT.
 */
exports.run = function(out) {
    out.log('-ENUM TESTS--------');

    // TODO: Add tests for Enum.

    out.log('---------------END-');
}
