//@module

var Options = require("modules/meta/options");

/**
 * Tests for the options module. Takes a Logger for OUT.
 */
exports.run = function(out) {
    out.log('-OPTIONS TESTS--------');

    // TODO: Add tests for Options.

    out.log('------------------END-');
}
